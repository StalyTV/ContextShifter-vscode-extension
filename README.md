# TaskSnap VSCode extension

### Packaging the Extension
Run `vsce package`. It creates a .vsix file in the repository root.

### Installing the Extension
Run `code --install-extension <path-to-extension>.vsix` to install the extension locally.

For more infos, see the [docs](https://code.visualstudio.com/api/working-with-extensions/publishing-extension#packaging-extensions).